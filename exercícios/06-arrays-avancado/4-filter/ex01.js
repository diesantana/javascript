//1.	Exercício 1: Filtrando uma matriz de números
//Dada uma matriz de números, filtre quaisquer números que sejam menores ou iguais a 0.

const numeros = [-2, -1, 0, 1, 2];
const subZero = numeros.filter( num => num  <= 0);
console.log(subZero);






