/*Exercício 1: Dobre cada elemento em uma matriz
Escreva uma função doubleArray() que receba um array de números como entrada e retorne um novo array onde cada elemento é dobrado usando o método map().
*/

const numeros = [1, 2, 3, 4, 5];

const dobro = numeros.map(num => num*2);
console.log(dobro, numeros);


array.reduce(callbackFn);
array.reduce(callbackFn, initialValue);








